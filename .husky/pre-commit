echo "🎨 Running pre-commit formatting..."

# Run formatting script
./format.sh

# Check if formatting changed any files
if ! git diff --quiet; then
    echo "⚠️  Files were modified by formatting. Please review and commit again."
    git diff --name-only
    exit 1
fi

echo "✅ Pre-commit formatting complete!"
